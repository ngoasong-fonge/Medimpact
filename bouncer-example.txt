[databases]
db1 = host=1.1.1.1 port=5432 dbname=db1 user=db1_user
db2 = host=1.1.1.1 port=5432 dbname=db2 user=db2_user

[pgbouncer]
listen_addr = 1.1.1.1
listen_port = 3333
auth_type = md5
auth_file = /etc/pgbouncer/userlist.txt
admin_users = admin_user
pool_mode = transaction
max_client_conn = 1000
default_pool_size = 20
logfile = /var/log/pgbouncer/pgbouncer.log
pidfile = /var/log/pgbouncer/pgbouncer.pid
unix_socket_dir = /tmp
ignore_startup_parameters = extra_float_digits


====================================
WITH SchemaSize AS (
  SELECT 
    ps.nspname AS schema_name,
    SUM(pg_total_relation_size(pc.oid)) AS total_size
  FROM 
    pg_class pc
  JOIN 
    pg_catalog.pg_namespace ps ON ps.oid = pc.relnamespace
  GROUP BY 
    ps.nspname
)
SELECT 
  ss.schema_name,
  pg_size_pretty(ss.total_size) AS total_size_pretty
FROM 
  SchemaSize ss
ORDER BY 
  ss.total_size DESC
LIMIT 10;


======================


citus=# DROP ROLE IF EXISTS pgaudit_owner;
ERROR:  role "pgaudit_owner" cannot be dropped because some objects depend on it
DETAIL:  privileges for database citus
owner of schema pgaudit
owner of table pgaudit.session
owner of table pgaudit.logon
owner of function pgaudit.logon_info()
owner of table pgaudit.log_event
owner of table pgaudit.audit_statement
owner of table pgaudit.audit_substatement
owner of table pgaudit.audit_substatement_detail
owner of view pgaudit.vw_audit_event
citus=# DROP ROLE IF EXISTS pgaudit_etl;
ERROR:  role "pgaudit_etl" cannot be dropped because some objects depend on it
DETAIL:  privileges for table pgaudit.session
privileges for column application_name of table pgaudit.session
privileges for table pgaudit.logon
privileges for column user_name of table pgaudit.logon
privileges for column last_success of table pgaudit.logon
privileges for column current_success of table pgaudit.logon
privileges for column last_failure of table pgaudit.logon
privileges for column failures_since_last_success of table pgaudit.logon
privileges for table pgaudit.log_event
privileges for table pgaudit.audit_statement
privileges for column state of table pgaudit.audit_statement
privileges for column error_session_line_num of table pgaudit.audit_statement
privileges for table pgaudit.audit_substatement
privileges for table pgaudit.audit_substatement_detail
